<CREOLE-DIRECTORY ID="${project.name}" VERSION="${project.version}" DESCRIPTION="${project.description}">
  <JAR scan="true">lib/${project.build.finalName}.jar</JAR>
  <JAR>lib/hfst.jar</JAR>
</CREOLE-DIRECTORY>